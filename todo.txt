2. compile and run without bootRun
3. add unit tests and integration tests with testcontainers
4. write down what IO is happening (database, endpoints, http clients etc)
5. keep properties off of project, add only default ones so it can run by default when somebody
    downloads the project
6. jwt
7. logging req/res
8. migrate to Spring WebFlux (with coroutines)